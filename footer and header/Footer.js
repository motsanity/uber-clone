import { Text, View } from "react-native-elements";
import React from "react";
import { FlatList } from "react-native-gesture-handler";

export default function Footer(){
    return(
        //create a footer with a text only
        <View>
            <Text>Footer</Text>
        </View>
    )
}